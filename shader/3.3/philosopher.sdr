#version 330

uniform mat4 xform;
uniform mat4 projection;

in float angle;

out VS_OUT { 
  vec2 size;
  float angle;
} board;

void main(void) {
  gl_Position = vec4(0.0, 0.0, 0.5, 1.0);
  board.size = vec2(0.2, 0.5);
  board.angle = angle;
}
